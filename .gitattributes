# The Polyboard parser splits strictly on CRLF (DetailsExtractorService),
# so the test fixtures must keep CRLF regardless of platform / core.autocrlf.
Kroiko.Testing/TestData/polyboard/*.txt text eol=crlf
